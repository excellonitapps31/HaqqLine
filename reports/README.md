# Phase reports

When a phase is deployed and its tests pass, add `phase-NN.md` in the form in `IMPLEMENTATION_PLAN.md` §7.

The next phase starts only after that report is in the repo and the phase is signed off.
